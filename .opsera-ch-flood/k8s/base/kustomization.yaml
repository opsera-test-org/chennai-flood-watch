apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yaml
  - service.yaml
  - ingress.yaml

labels:
  - pairs:
      app: ch-flood
      tenant: opsera
      managed-by: opsera
    includeSelectors: false
